3. Relacione V (verdadeiro) ou F (falso) para cada alternativa abaixo. Caso a alternativa seja falsa, justifique o 
erro. Não serão consideradas as alternativas sem justificativas ou com justificativas incorretas. 

("F") Fila e Pilha são políticas de inclusão e remoção numa lista que pode ser estática (array) ou dinâmica 
(encadeada). A diferença entre essas políticas é a forma como é realizada a remoção. Numa Pilha o primeiro 
elemento é retirado, já numa Fila é sempre o último elemento. 
// Numa Pilha o primeiro elemento é retirado, já numa Fila é sempre o último elemento.”

("F") Os algoritmos de ordenação MergeSort e QuickSort utilizam a técnica chamada divisão e conquista para 
subdividir os elementos do array que será ordenado. A principal diferença entre eles é que o MergeSort 
concatena as soluções obtidas para chegar à solução final, enquanto o QuickSort intercala. 
// Na verdade é o contrário. O MergeSort intercala os array divididos e o QuickSort concatena os arrays divididos.

("F") Em computação a busca de um valor numa lista pode ser feita de várias formas. Vimos em aula as buscas 
sequencial e binária que podem ser usadas sem qualquer restrição. Na busca sequencial cada elemento é 
comparado e por isso a complexidade de pior caso é O(n), já na busca binária, apenas parte dos elementos são 
comparados e por isso a complexidade de pior caso é O(1).  
// Na verdade a complexidade de pior caso da busca binaria eh de O(log n), sendo o log na base 2, pois a lista eh sempre dividada em dois

("F") O QuickSort tem a mesma complexidade de pior caso dos algoritmos BubbleSort e  SelectionSort,  ou  seja,  
O(n log n). 
// De fato o Quick tem a mesma complexidade de pior caso que os algoritmos citados porem, a complexidade eh de O(n^2)